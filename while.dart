void main() {
  int no = 20; // Initialization Count will start from here.
  
  // This is the condition
  while (no <= 30) {
    print("Number is $no");
    // This is the increment.
    no++;
  }
}
